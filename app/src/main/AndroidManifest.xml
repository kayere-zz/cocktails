<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cocktails">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cocktails">
        <activity android:name=".ui.drink_detail.DrinkImageActivity" android:theme="@style/Theme.cocktails.drinkImage"/>
        <activity android:name=".ui.ingredient_detail.IngredientDetailActivity"/>
        <activity android:name=".ui.home.HomeActivity" />
        <activity
            android:name=".ui.drink_detail.DrinkDetailActivity"
            android:theme="@style/Theme.CocktailsTranslucentStatusBar" />
        <activity
            android:name=".ui.splash_screen.SplashActivity"
            android:theme="@style/Theme.CocktailSplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>